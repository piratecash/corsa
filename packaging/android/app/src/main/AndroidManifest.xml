<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      android:enableOnBackInvokedCallback="false" keeps the LEGACY back
      dispatch alive. From targetSdk 36 (Android 16) the predictive-back
      migration is on by default and Activity.onBackPressed() is no
      longer called — which is the only path Gio v0.10 uses to deliver
      key.NameBack (app/os_android.go → GioView.onBack). Without this
      opt-out the system Back gesture would close the Activity instead of
      reaching handleBackNavigation, so it would never dismiss a context
      menu or leave a chat in the compact layout. Remove once Gio adopts
      OnBackInvokedCallback.
    -->
    <application
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true">
        <!--
          android:launchMode="singleTask" — NOT gogio's singleInstance.
          A singleInstance activity is the only member of its task and
          anything it starts opens in a task of its own, and an activity
          that cannot host another in its task cannot receive that
          activity's result: startActivityForResult returns RESULT_CANCELED
          at once. That is the whole of the system file picker
          (gioui.org/x/explorer), so Attach and Save as… could never come
          back with a file. singleTask keeps the same "one instance, at the
          root of its task" behaviour and lets the picker live in that task
          long enough to answer.
        -->
        <activity
            android:name="org.gioui.GioActivity"
            android:configChanges="screenSize|screenLayout|smallestScreenSize|orientation|keyboardHidden"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.GioApp"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
              corsa: links. BROWSABLE is what lets a link tapped in a
              browser, a mail client or another messenger reach us at
              all; DEFAULT is what makes the filter match an implicit
              VIEW intent. With launchMode="singleTask" above, a link
              tapped while the app runs arrives as onNewIntent (Gio turns
              it into an app.URLEvent); the intent that STARTS the app is
              read from the activity itself, because Gio may deliver that
              one before Go has an event iterator — see
              internal/app/desktop/deeplink_launch_android.go.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="corsa" />
            </intent-filter>
        </activity>
    </application>
</manifest>
